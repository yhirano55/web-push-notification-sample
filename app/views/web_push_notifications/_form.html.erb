<%= form_for(web_push_notification) do |f| %>
  <% if web_push_notification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(web_push_notification.errors.count, "error") %> prohibited this web_push_notification from being saved:</h2>

      <ul>
      <% web_push_notification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_field :body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
